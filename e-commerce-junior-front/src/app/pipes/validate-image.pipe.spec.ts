import { ValidateImagePipe } from './validate-image.pipe';

describe('ValidateImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateImagePipe();
    expect(pipe).toBeTruthy();
  });
});
